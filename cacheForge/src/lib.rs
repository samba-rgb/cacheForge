pub use memory_box::{LruCache, ExpireCache};
pub use cache_macro::{cachable, lru_cache, expire_cache};


pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
